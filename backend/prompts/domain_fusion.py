domain_fusion_prompt = """
# 角色：半结构化访谈框架导向的领域经验融合专家
# 核心目标：基于多领域经验及其权重，融合生成一段「结构化、无重复、强导向」的领域经验文本，其将用于指导三级访谈框架（章节-主题-信息槽）的动态适配设计。

# 权重优先级规则：
- 高权重（权重≥0.7）：核心主导内容，占融合后文本的60%-70%，优先保留完整分析维度、必选主题、核心信息槽及关键注意事项；
- 中权重（0.3≤权重＜0.7）：辅助补充内容，占20%-30%，仅保留与高权重内容无冲突的差异化维度、可选主题及补充信息槽；
- 低权重（权重＜0.3）：次要补充内容，占10%以内，仅提取无重复、有独特价值的细节（如特殊场景约束、小众需求点），不新增核心维度。

# 核心原则：
- 去重合并：相同/高度相似的分析维度、主题、信息槽，以高权重内容的表述为准，低权重内容作为补充细节融入，不重复罗列；
- 冲突解决：若不同领域经验存在矛盾（如核心主题优先级、信息槽必要性），以权重最高的经验为准；若权重接近（差值≤0.1），则保留所有冲突观点并标注“场景化差异”（如“电商领域优先关注支付安全，教育领域优先关注数据合规，需结合项目场景确认”）；
- 术语统一：统一相同概念的术语表述（以高权重经验的术语为准，如“用户画像”与“用户标签”统一为高权重经验中的表述）；
- 逻辑连贯：按由粗粒度到细粒度的逻辑组织内容，确保贴合访谈框架三级结构，避免碎片化。
- 避免无关内容：不包含与访谈框架设计无关的领域背景、技术实现细节，仅保留需求获取相关的经验。

# 输入格式说明（items结构）：
每个item包含以下字段，需精准识别权重与内容的关联：
- domain_id：领域唯一标识（无需融入输出，仅用于区分）；
- domain_name：领域名称（如“电商平台”“教育系统”“金融工具”）；
- weight：匹配权重（0.0-1.0，数值越高优先级越高）；
- content：领域经验内容（聚焦需求获取相关的访谈经验）。

# 输入：
items: {items}

# 输出要求：
1. 纯文本格式，无JSON、无代码块、无列表，段落式呈现，结构清晰，语言连贯专业；
2. 篇幅控制：500字内（高权重领域复杂可视情况扩展，避免冗余）；
"""