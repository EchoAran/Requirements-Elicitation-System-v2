web_search_query_prompt = """
# 角色：你是检索规划助手。
# 任务：现在需要进行半结构化访谈的前期规划，需要从网络上检索相应的资料，请你根据[项目需求]生成用于联网检索的查询词。

# 输入：
## 项目名称:
{project_name}

## 项目需求:
{initial_requirements}

# 要求:
1. 生成3-5条中文检索词
2. 单条检索词长度不超过30字，表述简洁精准
3. 仅输出标准JSON数组字符串，无额外文字、解释、格式符号
4. 示例输出：["检索词1","检索词2","检索词3"]
"""


web_page_clean_prompt = """
# 角色：网页内容清洗结构化提取助手
# 任务：根据[项目需求]中的内容，对网页本文进行分析，去除杂乱无用的文字，整理出与之相关的主要信息。

# 输入
## 项目名称:
{project_name}

## 项目需求:
{initial_requirements}

## 网页URL:
{url}

## 网页文本:
'''
{page_text}
'''

# 核心规则
1. 可用性判定：若存在以下情况（任一），则认为该网页无效，不需要整理信息，直接认为accept=false，否则accept=true
   - 乱码
   - 为广告/无意义内容，无匹配需求的有效信息
   - 404/空白/拦截页，无任何可读内容
   - 完全不涉及[项目需求]相关信息
2. 提取规则：仅提取与项目需求强相关内容，100%基于原文，无篡改、无不相关内容、无冗余内容

# 输出字段（严格JSON格式）
- accept: boolean（必填，仅true/false）
- title: string（网页核心标题，50字内，无则填"-"）
- key_insights: string（需求相关核心洞察，500字内，无则填"-"）
- content: string（清洗后的有效正文，无则填"-"）
- tags: string[]（3-8个相关关键词，无则填[]）

# 强制约束
- accept=false时，其余字段统一填"-"和[]
- 必须转义JSON特殊字符，保证格式100%可解析
- 禁止输出任何JSON以外的内容

# 声明：
- [网页文本]是基于[项目需求]从网络上爬取的背景知识，用于项目的访谈框架制定，可能存在乱码网页、无关网页、不可用网页，所以需要你甄别；
- [网页文本]可能存在广告、操作UI、声明等与正文内容无关的文字，你需要过滤掉这些内容。
"""


domain_generation_prompt = """
你是资深行业顾问。请基于项目需求生成一条结构化领域经验。

项目名称:
{project_name}

项目需求:
{initial_requirements}

要求:
1. 聚焦核心业务逻辑、常见风险、功能模块建议、落地约束。
2. 输出JSON对象，字段:
   - title: string
   - key_insights: string
   - content: string
   - tags: string[]
3. 不要输出额外解释。
"""

knowledge_fusion_prompt = """
你是领域知识融合专家。请将多个来源的知识项融合为可用于访谈框架生成的领域经验正文。

项目名称:
{project_name}

项目需求:
{initial_requirements}

知识项(JSON):
{knowledge_items}


融合步骤：
1. 先按相关度与可信度排序，优先处理与项目需求强相关的条目；
2. 将条目映射到统一结构：业务目标、用户场景、关键流程、约束风险、验收关注点；
3. 去重并合并相同结论，保留信息增量；
4. 对冲突内容做条件化整合，保留前提差异；
5. 输出一段连贯文本，能够直接指导“章节/主题/槽位”设计。

处理原则：
- 不做来源罗列式拼接，不生成“摘要列表感”文本；
- 不引入无依据新事实，不放大低可信零散信息；
- 保留对访谈设计最有用的信息：可提问、可验证、可落地；
- 弱化营销口径、术语噪音和实现细枝末节；
- 术语统一，避免同一概念多套表述。

要求:
1. 统一术语，去重冲突，保留对需求澄清最有价值的信息。
2. 输出纯文本，不要JSON，不要代码块，不要列表符号。
3. 长度控制在900字内。
"""
